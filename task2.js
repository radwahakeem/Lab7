/* function MaxOfArray(...numArray) {
    var max= Math.max(...numArray);
    var index = numArray.indexOf(max);
    document.write("max : ", numArray[index]);
  }
  MaxOfArray(1,2,3);

  document.write("<br>")

  function MinOfArray(...numArray) {
    var min= Math.min(...numArray);
    var index = numArray.indexOf(min);
    document.write("min : ", numArray[index]);
  }
  MinOfArray(1,2,3); */